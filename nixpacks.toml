# Pin Node 22 LTS so better-sqlite3 finds prebuilt binaries
# (Node 24 has no prebuilds yet → tries to compile from source,
#  which needs Python in the build image and fails on Railway/Nixpacks.)
[phases.setup]
nixPkgs = ['nodejs_22', 'npm-10_x']
